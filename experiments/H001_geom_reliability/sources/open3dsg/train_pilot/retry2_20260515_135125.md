# Open3DSG Checkpoint Pilot Training Retry2

Status: failed
Launched at: 2026-05-15 13:51 KST
Finished at: 2026-05-15 14:00 KST
Exit code: 1

## Reason

The first pilot failed with CUDA OOM at epoch 0 step 1419/3744 during BLIP
projector forward. The first retry with chunk size 4 failed at epoch 0 step
235/3744 while an unrelated `conceptgraph` container expanded GPU usage. This
retry uses the same source patch schema `h001_open3dsg_source_patch_v5`, but
sets `OPEN3DSG_BLIP_PROJECTOR_CHUNK_SIZE=1` and relies on preflight schema
`h001_open3dsg_training_preflight_v6` to require GPU free memory before heavy
training starts.

## Working Directory

```text
/home/yoohyun/research
```

## Exact Command

```bash
mkdir -p logs experiments/H001_geom_reliability/sources/open3dsg/train_pilot
tmux new-session -d -s h001_open3dsg_train_pilot_retry2 "cd /home/yoohyun/research && bash -lc 'set -o pipefail; echo \"started_at=\$(date -Is)\"; echo \"cwd=\$(pwd)\"; echo \"gpu_before=\"; nvidia-smi --query-gpu=timestamp,index,name,memory.total,memory.used,memory.free,utilization.gpu --format=csv,noheader,nounits || true; echo \"command=sg docker -c env UID=\$(id -u) GID=\$(id -g) OPEN3DSG_TRAIN_WORKERS=0 OPEN3DSG_BLIP_PROJECTOR_CHUNK_SIZE=1 OPEN3DSG_BLIP_EMBED_CHUNK_SIZE=2 OPEN3DSG_MIN_GPU_FREE_MB=18000 PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True,max_split_size_mb:64 docker compose -f experiments/H001_geom_reliability/sources/open3dsg/compose.open3dsg.yaml run --rm train_pilot\"; sg docker -c '\''env UID=\$(id -u) GID=\$(id -g) OPEN3DSG_TRAIN_WORKERS=0 OPEN3DSG_BLIP_PROJECTOR_CHUNK_SIZE=1 OPEN3DSG_BLIP_EMBED_CHUNK_SIZE=2 OPEN3DSG_MIN_GPU_FREE_MB=18000 PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True,max_split_size_mb:64 docker compose -f experiments/H001_geom_reliability/sources/open3dsg/compose.open3dsg.yaml run --rm train_pilot'\''; rc=\$?; echo \"finished_at=\$(date -Is)\"; echo \"exit_code=\$rc\"; printf \"%s\n\" \"\$rc\" > logs/open3dsg_train_pilot_retry2_20260515_135125.exit; exit \$rc' > logs/open3dsg_train_pilot_retry2_20260515_135125.log 2>&1"
```

## Log And Exit Files

```text
logs/open3dsg_train_pilot_retry2_20260515_135125.log
logs/open3dsg_train_pilot_retry2_20260515_135125.exit
```

## Verification Commands

After the background job exits:

```bash
cat logs/open3dsg_train_pilot_retry2_20260515_135125.exit
find local_dataset/Open3DSG_staged/training_repro -type f -name '*.ckpt' -printf '%TY-%Tm-%Td %TH:%TM:%TS %s %p\n' | sort | tail -20
```

If a checkpoint exists, refresh checkpoint provenance/selection before raw dump,
evaluation, or full training.

## Launch Check

Checked at: 2026-05-15 13:52 KST

```text
tmux session: active
Docker container: open3dsg-train_pilot-run-755150572fe2 Up
exit file: not present
latest log tail: training loop entered, epoch 0 step 1/3744
GPU processes: conceptgraph 580 MiB, Open3DSG python 12714 MiB
```

## Result

Checked at: 2026-05-15 16:00 KST

The retry reached epoch 0 step 699/3744 and failed with CUDA OOM in the
chunked BLIP projector path:

```text
torch.OutOfMemoryError: CUDA out of memory
```

The OOM happened even with `OPEN3DSG_BLIP_PROJECTOR_CHUNK_SIZE=1`. No `.ckpt`
checkpoint was found under the staged training root. Do not refresh checkpoint
provenance/selection or launch full training from this retry.

Next action: inspect a lower-memory checkpoint route before another heavy
retry, such as disabling train-time BLIP projector gradients, using averaged
BLIP embeddings if compatible with the dumped features, or using the reduced
checkpoint smoke route as non-paper evidence.
