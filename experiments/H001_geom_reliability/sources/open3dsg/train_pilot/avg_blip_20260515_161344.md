# Open3DSG Checkpoint Pilot Training Avg-BLIP

Status: completed
Launched at: 2026-05-15 16:13 KST
Finished at: 2026-05-15 16:47 KST
Exit code: 0

## Reason

The non-averaged BLIP projector route failed three checkpoint-smoke attempts
with CUDA OOM and produced no checkpoint. This pilot uses the lower-memory
Open3DSG CLI option `--avg_blip_emb`, which is compatible with the official
BLIP feature tensors and skips the train-time BLIP projector path.

## Working Directory

```text
/home/yoohyun/research
```

## Exact Command

```bash
mkdir -p logs experiments/H001_geom_reliability/sources/open3dsg/train_pilot
tmux new-session -d -s h001_open3dsg_train_pilot_avg_blip "cd /home/yoohyun/research && bash -lc 'set -o pipefail; echo \"started_at=\$(date -Is)\"; echo \"cwd=\$(pwd)\"; echo \"gpu_before=\"; nvidia-smi --query-gpu=timestamp,index,name,memory.total,memory.used,memory.free,utilization.gpu --format=csv,noheader,nounits || true; echo \"command=sg docker -c env UID=\$(id -u) GID=\$(id -g) OPEN3DSG_TRAIN_WORKERS=0 OPEN3DSG_MIN_GPU_FREE_MB=18000 PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True,max_split_size_mb:64 docker compose -f configs/open3dsg/compose.open3dsg.yaml run --rm train_pilot_avg_blip\"; sg docker -c '\''env UID=\$(id -u) GID=\$(id -g) OPEN3DSG_TRAIN_WORKERS=0 OPEN3DSG_MIN_GPU_FREE_MB=18000 PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True,max_split_size_mb:64 docker compose -f configs/open3dsg/compose.open3dsg.yaml run --rm train_pilot_avg_blip'\''; rc=\$?; echo \"finished_at=\$(date -Is)\"; echo \"exit_code=\$rc\"; printf \"%s\n\" \"\$rc\" > logs/open3dsg_train_pilot_avg_blip_20260515_161344.exit; exit \$rc' > logs/open3dsg_train_pilot_avg_blip_20260515_161344.log 2>&1"
```

## Log And Exit Files

```text
logs/open3dsg_train_pilot_avg_blip_20260515_161344.log
logs/open3dsg_train_pilot_avg_blip_20260515_161344.exit
```

## Verification Commands

After the background job exits:

```bash
cat logs/open3dsg_train_pilot_avg_blip_20260515_161344.exit
find local_dataset/Open3DSG_staged/training_repro -type f -name '*.ckpt' -printf '%TY-%Tm-%Td %TH:%TM:%TS %s %p\n' | sort | tail -20
```

If a checkpoint exists, refresh checkpoint provenance/selection before raw dump,
evaluation, or full training.

## Launch Check

Checked at: 2026-05-15 16:14 KST

```text
tmux session: active
Docker container: open3dsg-train_pilot_avg_blip-run-ad64be0a3ee8 Up
exit file: not present
latest log tail: training loop entered, epoch 0 step 2/3744
model params: 34.9M trainable
GPU process: Open3DSG python 7126 MiB
```

## Result

Checked at: 2026-05-15 17:26 KST

```text
exit file: 0
epoch: 0/1 completed
global step: 936
val/loss: 0.37145
checkpoint candidates:
- local_dataset/Open3DSG_staged/training_repro/mlops/opensg/mlflow/363094050435167554/80dd96ad1d9949ac98cd5abd5c62fed0/checkpoints/epoch=0-step=936.ckpt
- local_dataset/Open3DSG_staged/training_repro/mlops/opensg/mlflow/363094050435167554/80dd96ad1d9949ac98cd5abd5c62fed0/checkpoints/last.ckpt
```

Checkpoint provenance/selection was refreshed with schema
`h001_open3dsg_checkpoint_selection_v2`. The two checkpoints are recorded as
`avg_blip_pilot` with `paper_result_eligible=false`, so they validate the
checkpoint route but are not final paper-result checkpoints.
