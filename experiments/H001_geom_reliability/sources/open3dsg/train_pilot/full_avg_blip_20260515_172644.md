# Open3DSG Full Training Avg-BLIP

Status: launched
Launched at: 2026-05-15 17:26 KST

## Reason

The avg-BLIP pilot completed successfully with exit code 0 and produced two
`avg_blip_pilot` checkpoints. Those pilot checkpoints are not paper-result
eligible, so the next route is full avg-BLIP training using the same official
feature dump and Open3DSG training script variant.

## Working Directory

```text
/home/yoohyun/research
```

## Exact Command

```bash
mkdir -p logs experiments/H001_geom_reliability/sources/open3dsg/train_pilot
tmux new-session -d -s h001_open3dsg_train_full_avg_blip "cd /home/yoohyun/research && bash -lc 'set -o pipefail; echo \"started_at=\$(date -Is)\"; echo \"cwd=\$(pwd)\"; echo \"gpu_before=\"; nvidia-smi --query-gpu=timestamp,index,name,memory.total,memory.used,memory.free,utilization.gpu --format=csv,noheader,nounits || true; echo \"command=sg docker -c env UID=\$(id -u) GID=\$(id -g) OPEN3DSG_TRAIN_WORKERS=0 OPEN3DSG_MIN_GPU_FREE_MB=18000 PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True,max_split_size_mb:64 docker compose -f experiments/H001_geom_reliability/sources/open3dsg/compose.open3dsg.yaml run --rm train_full_avg_blip\"; sg docker -c '\''env UID=\$(id -u) GID=\$(id -g) OPEN3DSG_TRAIN_WORKERS=0 OPEN3DSG_MIN_GPU_FREE_MB=18000 PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True,max_split_size_mb:64 docker compose -f experiments/H001_geom_reliability/sources/open3dsg/compose.open3dsg.yaml run --rm train_full_avg_blip'\''; rc=\$?; echo \"finished_at=\$(date -Is)\"; echo \"exit_code=\$rc\"; printf \"%s\n\" \"\$rc\" > logs/open3dsg_train_full_avg_blip_20260515_172644.exit; exit \$rc' > logs/open3dsg_train_full_avg_blip_20260515_172644.log 2>&1"
```

## Log And Exit Files

```text
logs/open3dsg_train_full_avg_blip_20260515_172644.log
logs/open3dsg_train_full_avg_blip_20260515_172644.exit
```

## Expected Outputs

```text
local_dataset/Open3DSG_staged/training_repro/mlops/opensg/mlflow/<experiment-id>/<run-id>/checkpoints/*.ckpt
```

## Verification Commands

After the background job exits:

```bash
cat logs/open3dsg_train_full_avg_blip_20260515_172644.exit
find local_dataset/Open3DSG_staged/training_repro/mlops/opensg/mlflow -type f -name '*.ckpt' -printf '%TY-%Tm-%Td %TH:%TM:%TS %s %p\n' | sort | tail -30
sg docker -c 'env UID=$(id -u) GID=$(id -g) docker compose -f experiments/H001_geom_reliability/compose.yaml run --rm open3dsg_checkpoint_selection'
```

## Claim Boundary

If this succeeds, it is an explicitly labeled averaged-BLIP Open3DSG full
variant. It is not the exact non-averaged BLIP projector route. Do not run raw
dump or H001 held-out metrics until checkpoint provenance/selection is
refreshed after the full run completes.

## Launch Check

Checked at: 2026-05-15 17:28 KST

```text
tmux session: active
Docker container: open3dsg-train_full_avg_blip-run-b642ae11a484 Up
exit file: not present
latest log tail: training loop entered, epoch 0 step 17/3744
mlflow run id: 2a23a9af581b4666a207423aa6217853
model params: 34.9M trainable
GPU processes: Jupyter kernel 10704 MiB, Open3DSG python 2010 MiB
```
