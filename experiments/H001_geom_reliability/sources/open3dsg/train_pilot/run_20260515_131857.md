# Open3DSG Checkpoint Pilot Training

Status: failed
Launched at: 2026-05-15 13:18 KST
Finished at: 2026-05-15 13:33 KST
Exit code: 1

## Working Directory

```text
/home/yoohyun/research
```

## Preconditions

```text
Docker feature_audit status: ready
official feature coverage: 3900/3900 complete ids
train split: 3744/3744 complete feature ids
validation split: 156/156 complete feature ids
missing preprocessed: 0
```

## Exact Command

```bash
mkdir -p logs experiments/H001_geom_reliability/sources/open3dsg/train_pilot
tmux new-session -d -s h001_open3dsg_train_pilot "cd /home/yoohyun/research && bash -lc 'set -o pipefail; echo \"started_at=\$(date -Is)\"; echo \"cwd=\$(pwd)\"; echo \"gpu_before=\"; nvidia-smi --query-gpu=timestamp,index,name,memory.total,memory.used,memory.free,utilization.gpu --format=csv,noheader,nounits || true; echo \"command=sg docker -c env UID=\$(id -u) GID=\$(id -g) OPEN3DSG_TRAIN_WORKERS=0 PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True,max_split_size_mb:128 docker compose -f experiments/H001_geom_reliability/sources/open3dsg/compose.open3dsg.yaml run --rm train_pilot\"; sg docker -c '\''env UID=\$(id -u) GID=\$(id -g) OPEN3DSG_TRAIN_WORKERS=0 PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True,max_split_size_mb:128 docker compose -f experiments/H001_geom_reliability/sources/open3dsg/compose.open3dsg.yaml run --rm train_pilot'\''; rc=\$?; echo \"finished_at=\$(date -Is)\"; echo \"exit_code=\$rc\"; printf \"%s\n\" \"\$rc\" > logs/open3dsg_train_pilot_20260515_131857.exit; exit \$rc' > logs/open3dsg_train_pilot_20260515_131857.log 2>&1"
```

## Log And Exit Files

```text
logs/open3dsg_train_pilot_20260515_131857.log
logs/open3dsg_train_pilot_20260515_131857.exit
```

## Expected Outputs

```text
local_dataset/Open3DSG_staged/training_repro/output/checkpoints/
local_dataset/Open3DSG_staged/training_repro/mlops/opensg/mlflow/
```

Pilot training is a checkpoint-smoke gate. It does not create H001 metric
evidence and must not be selected with held-out H001 metric or failure-analysis
results.

## Verification Commands

After the background job exits:

```bash
cat logs/open3dsg_train_pilot_20260515_131857.exit
find local_dataset/Open3DSG_staged/training_repro -type f -name '*.ckpt' -printf '%TY-%Tm-%Td %TH:%TM:%TS %s %p\n' | sort | tail -20
```

If a checkpoint exists, refresh the checkpoint provenance/selection artifact
before raw dump or evaluation.

## Result

The pilot reached epoch 0 step 1419/3744 and failed with CUDA OOM in
`open3dsg/models/sgpn.py` during BLIP projector forward:

```text
torch.OutOfMemoryError: CUDA out of memory
```

No `.ckpt` checkpoint was found under the staged training root after this run.
Do not launch full training from this failed pilot.

Next action: apply a chunked BLIP projector patch and retry the pilot before
checkpoint provenance/selection.
