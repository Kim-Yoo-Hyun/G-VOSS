# Open3DSG Feature Dump Resume

Status: launched
Launched at: 2026-05-14 14:21 KST

Status check: 2026-05-14 16:48 KST

- `tmux h001_open3dsg_dump_features` is no longer visible.
- Docker container `open3dsg-dump_features_3rscan-run-b2c033e45fa0` is still running.
- GPU process PID 725501 is active.
- Exit file is not present.
- Feature coverage is 3352/3900 complete ids.
- Do not launch another resume while this container is running.

## Working Directory

```text
/home/yoohyun/research
```

## Exact Command

```bash
mkdir -p logs
tmux new-session -d -s h001_open3dsg_dump_features "cd /home/yoohyun/research && bash -lc 'set -o pipefail; echo \"started_at=\$(date -Is)\"; echo \"cwd=\$(pwd)\"; echo \"gpu_before=\"; nvidia-smi --query-gpu=timestamp,index,name,memory.total,memory.used,memory.free,utilization.gpu --format=csv,noheader,nounits || true; echo \"command=sg docker -c env UID=$(id -u) GID=$(id -g) OPEN3DSG_DUMP_WORKERS=0 OPEN3DSG_BLIP_EMBED_CHUNK_SIZE=2 PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True,max_split_size_mb:128 docker compose -f experiments/H001_geom_reliability/sources/open3dsg/compose.open3dsg.yaml run --rm dump_features_3rscan\"; sg docker -c '\''env UID=$(id -u) GID=$(id -g) OPEN3DSG_DUMP_WORKERS=0 OPEN3DSG_BLIP_EMBED_CHUNK_SIZE=2 PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True,max_split_size_mb:128 docker compose -f experiments/H001_geom_reliability/sources/open3dsg/compose.open3dsg.yaml run --rm dump_features_3rscan'\''; rc=$?; echo \"finished_at=\$(date -Is)\"; echo \"exit_code=$rc\"; printf \"%s\n\" \"$rc\" > logs/open3dsg_dump_features_resume_20260514_142104.exit; exit $rc' > logs/open3dsg_dump_features_resume_20260514_142104.log 2>&1"
```

## Log And Exit Files

```text
logs/open3dsg_dump_features_resume_20260514_142104.log
logs/open3dsg_dump_features_resume_20260514_142104.exit
```

## Output Path

```text
local_dataset/Open3DSG_staged/training_repro/output/features/clip_features_h001_official_blip_top5_scales3/
```

## Expected Files

Each expected feature id should have one `.pt` file in each role directory:

```text
export_obj_clip_valids/
export_rel_clip_emb_clip_BLIP_Topk_5_scales_3_vis_crit_0.19999999999999998/
export_obj_clip_emb_clip_OpenSeg_Topk_5_scales_3_vis_crit_0.19999999999999998_vis_crit_mask_0.1/
```

Expected completion:

```text
3900 complete feature ids = 3744 train + 156 validation
```

Launch state:

```text
3329/3900 complete feature ids before resume
571 feature ids remain
```

## Memory Mitigation

This resume keeps the official BLIP TopK5/scales3 route and applies only runtime/source memory mitigation:

```text
OPEN3DSG_BLIP_EMBED_CHUNK_SIZE=2
PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True,max_split_size_mb:128
```

The source patch schema is `h001_open3dsg_source_patch_v4`; it chunks BLIP image embedding to reduce peak GPU memory without changing the official feature route.

## Verification Command

After the background job exits:

```bash
sg docker -c 'env UID=$(id -u) GID=$(id -g) docker compose -f experiments/H001_geom_reliability/sources/open3dsg/compose.open3dsg.yaml run --rm feature_audit'
```

Lightweight progress check:

```bash
find local_dataset/Open3DSG_staged/training_repro/output/features/clip_features_h001_official_blip_top5_scales3 -mindepth 1 -maxdepth 1 -type d -printf '%f ' -exec sh -c 'printf "%s\n" "$(find "$1" -type f -name "*.pt" | wc -l)"' sh {} \;
```

## Policy

Do not continuously monitor this job. Check progress only when requested or
when the next dependent task needs completion.
