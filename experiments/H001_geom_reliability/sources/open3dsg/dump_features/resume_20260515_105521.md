# Open3DSG Feature Dump Resume

Status: launched
Launched at: 2026-05-15 10:55 KST

## Working Directory

```text
/home/yoohyun/research
```

## Start State

```text
3883/3900 complete feature ids before resume
17 complete feature ids remain
```

At launch, the three feature role directories were not fully balanced:

```text
export_obj_clip_valids: 3883
export_rel_clip_emb_clip_BLIP_Topk_5_scales_3_vis_crit_0.19999999999999998: 3883
export_obj_clip_emb_clip_OpenSeg_Topk_5_scales_3_vis_crit_0.19999999999999998_vis_crit_mask_0.1: 3884
```

The previous Docker container was no longer present, no GPU process was active,
and `logs/open3dsg_dump_features_resume_20260514_142104.exit` did not exist.

## Exact Command

```bash
mkdir -p logs
tmux new-session -d -s h001_open3dsg_dump_features "cd /home/yoohyun/research && bash -lc 'set -o pipefail; echo \"started_at=\$(date -Is)\"; echo \"cwd=\$(pwd)\"; echo \"gpu_before=\"; nvidia-smi --query-gpu=timestamp,index,name,memory.total,memory.used,memory.free,utilization.gpu --format=csv,noheader,nounits || true; echo \"command=sg docker -c env UID=\$(id -u) GID=\$(id -g) OPEN3DSG_DUMP_WORKERS=0 OPEN3DSG_BLIP_EMBED_CHUNK_SIZE=2 PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True,max_split_size_mb:128 docker compose -f experiments/H001_geom_reliability/sources/open3dsg/compose.open3dsg.yaml run --rm dump_features_3rscan\"; sg docker -c '\''env UID=\$(id -u) GID=\$(id -g) OPEN3DSG_DUMP_WORKERS=0 OPEN3DSG_BLIP_EMBED_CHUNK_SIZE=2 PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True,max_split_size_mb:128 docker compose -f experiments/H001_geom_reliability/sources/open3dsg/compose.open3dsg.yaml run --rm dump_features_3rscan'\''; rc=\$?; echo \"finished_at=\$(date -Is)\"; echo \"exit_code=\$rc\"; printf \"%s\n\" \"\$rc\" > logs/open3dsg_dump_features_resume_20260515_105521.exit; exit \$rc' > logs/open3dsg_dump_features_resume_20260515_105521.log 2>&1"
```

## Log And Exit Files

```text
logs/open3dsg_dump_features_resume_20260515_105521.log
logs/open3dsg_dump_features_resume_20260515_105521.exit
```

## Output Path

```text
local_dataset/Open3DSG_staged/training_repro/output/features/clip_features_h001_official_blip_top5_scales3/
```

## Expected Files

Each expected feature id should have one `.pt` file in each role directory:

```text
export_obj_clip_valids/
export_rel_clip_emb_clip_BLIP_Topk_5_scales_3_vis_crit_0.19999999999999998/
export_obj_clip_emb_clip_OpenSeg_Topk_5_scales_3_vis_crit_0.19999999999999998_vis_crit_mask_0.1/
```

Expected completion:

```text
3900 complete feature ids = 3744 train + 156 validation
```

## Verification Command

After the background job exits:

```bash
sg docker -c 'env UID=$(id -u) GID=$(id -g) docker compose -f experiments/H001_geom_reliability/sources/open3dsg/compose.open3dsg.yaml run --rm feature_audit'
```

Lightweight progress check:

```bash
find local_dataset/Open3DSG_staged/training_repro/output/features/clip_features_h001_official_blip_top5_scales3 -type f -name '*.pt' -printf '%f\n' | sed 's/\.[^.]*$//' | sort | uniq -c | awk '$1==3{c++} END{print c+0}'
```
