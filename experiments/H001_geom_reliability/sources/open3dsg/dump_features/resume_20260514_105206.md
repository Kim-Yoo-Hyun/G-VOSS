# Open3DSG Feature Dump Resume

Status: failed
Launched at: 2026-05-14 10:52 KST
Finished at: 2026-05-14 12:47 KST
Exit code: 1

## Working Directory

```text
/home/yoohyun/research
```

## Exact Command

```bash
mkdir -p logs
tmux new-session -d -s h001_open3dsg_dump_features "cd /home/yoohyun/research && bash -lc 'set -o pipefail; echo \"started_at=\$(date -Is)\"; echo \"cwd=\$(pwd)\"; echo \"command=sg docker -c env UID=$(id -u) GID=$(id -g) OPEN3DSG_DUMP_WORKERS=0 docker compose -f experiments/H001_geom_reliability/sources/open3dsg/compose.open3dsg.yaml run --rm dump_features_3rscan\"; sg docker -c '\''env UID=$(id -u) GID=$(id -g) OPEN3DSG_DUMP_WORKERS=0 docker compose -f experiments/H001_geom_reliability/sources/open3dsg/compose.open3dsg.yaml run --rm dump_features_3rscan'\''; rc=$?; echo \"finished_at=\$(date -Is)\"; echo \"exit_code=$rc\"; printf \"%s\n\" \"$rc\" > logs/open3dsg_dump_features_resume_20260514_105206.exit; exit $rc' > logs/open3dsg_dump_features_resume_20260514_105206.log 2>&1"
```

## Log And Exit Files

```text
logs/open3dsg_dump_features_resume_20260514_105206.log
logs/open3dsg_dump_features_resume_20260514_105206.exit
```

## Output Path

```text
local_dataset/Open3DSG_staged/training_repro/output/features/clip_features_h001_official_blip_top5_scales3/
```

## Expected Files

Each expected feature id should have one `.pt` file in each role directory:

```text
export_obj_clip_valids/
export_rel_clip_emb_clip_BLIP_Topk_5_scales_3_vis_crit_0.19999999999999998/
export_obj_clip_emb_clip_OpenSeg_Topk_5_scales_3_vis_crit_0.19999999999999998_vis_crit_mask_0.1/
```

Expected completion:

```text
3900 complete feature ids = 3744 train + 156 validation
```

Launch state:

```text
3329/3900 complete feature ids before resume
```

Observed result:

```text
3329/3900 complete feature ids after exit
Failure: CUDA out of memory during BLIP vision forward
```

Previous attempt:

```text
logs/open3dsg_dump_features_resume_20260512_005606.exit = 137
stopped at 2026-05-13 10:18 KST
```

## Verification Command

After the background job exits:

```bash
sg docker -c 'env UID=$(id -u) GID=$(id -g) docker compose -f experiments/H001_geom_reliability/sources/open3dsg/compose.open3dsg.yaml run --rm feature_audit'
```

Lightweight progress check:

```bash
find local_dataset/Open3DSG_staged/training_repro/output/features/clip_features_h001_official_blip_top5_scales3 -mindepth 1 -maxdepth 1 -type d -printf '%f ' -exec sh -c 'printf "%s\n" "$(find "$1" -type f -name "*.pt" | wc -l)"' sh {} \;
```

## Policy

Do not continuously monitor this job. Check progress only when requested or
when the next dependent task needs completion.
