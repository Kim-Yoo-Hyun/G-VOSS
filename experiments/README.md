# Experiments

The public submission tree contains one active experiment root:

- RelCompat3D_geom_reliability/: RelCompat3D protocols, model locks, compact
  evaluations, and reproduction commands.

H002 and all superseded or optional experiment branches are preserved in the
ignored local archive. They are not part of the RelCompat3D paper claim or public
submission artifact.

Executable code belongs in src/relcompat3d/, Docker services in configs/relcompat3d/,
shell orchestration in scripts/, and compact result navigation in results/.
Before rerunning or deleting RelCompat3D artifacts, read docs/reproducibility.md and
experiments/RelCompat3D_geom_reliability/README.md.
