\section{Conclusion}

\method{} separates predicate--geometry compatibility from the source relation
score and enforces the defined proximity and vertical endpoint/predicate
transformations. Across three predictors on a shared 3DSSG target, family-aware
re-ranking yields lower verifier-derived Violation point estimates without
reducing Recall point estimates at $K=10$--$50$, while support/contact candidates
remain in source order.
